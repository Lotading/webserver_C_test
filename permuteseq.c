#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct {
    char *seq;
} permuteseq;

char *getPermutation(int n, int k) {
    int iterations;

    permuteseq *Permuteseq;  
    Permuteseq->seq = malloc(Permuteseq);
    char* pSeq = &Permuteseq->seq;

    char *nStr = n;
    for (size_t i = 0; i < n; i++)
    {
        nStr[i]
    }
    return Permuteseq->seq;
}